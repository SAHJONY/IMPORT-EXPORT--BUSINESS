{
  "version": 2,
  "builds": [
    { "src": "fastapi_server.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "fastapi_server.py" }
  ]
}
