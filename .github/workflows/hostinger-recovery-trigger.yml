name: Trigger Hostinger Docker Recovery (Retired V3)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement
        run: |
          echo 'HOSTINGER_RECOVERY_V3_TRIGGER_RETIRED=1'
          echo 'Use bounded Hostinger WhatsApp Recovery V6. V3 is no longer auto-dispatched.'
