name: Hostinger Recovery One-Time Handoff [RETIRED]

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo HOSTINGER_RECOVERY_HANDOFF_RETIRED=1
          echo USE_HOSTINGER_WHATSAPP_RECOVERY_V7=1
