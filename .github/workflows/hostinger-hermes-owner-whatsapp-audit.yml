name: Hostinger Hermes Owner WhatsApp Audit

on:
  push:
    branches: [main]
    paths: ['.github/workflows/hostinger-hermes-owner-whatsapp-audit.yml']

jobs:
  audit:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    env:
      HOST: 69.62.68.67
      SSH_USER: root
      PRIVATE_KEY: ${{ secrets.HOSTINGER_SSH_PRIVATE_KEY }}
    steps:
      - name: Prepare SSH
        shell: bash
        run: |
          set -euo pipefail
          install -d -m 700 ~/.ssh
          printf '%s\n' "$PRIVATE_KEY" > ~/.ssh/key
          chmod 600 ~/.ssh/key
          ssh-keygen -y -f ~/.ssh/key >/dev/null
      - name: Inspect owner routing injection point
        shell: bash
        run: |
          set -euo pipefail
          ssh -o BatchMode=yes -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o ConnectTimeout=12 -i ~/.ssh/key "$SSH_USER@$HOST" 'bash -s' <<'REMOTE'
          set -euo pipefail
          echo '=== WHATSAPP INCOMING 1190-1275 ==='
          sed -n '1190,1275p' /root/.hermes/hermes-agent/gateway/platforms/whatsapp.py
          echo '=== INCOMING MODEL CLASS ==='
          grep -Rni --exclude-dir='__pycache__' -E 'class IncomingMessage|channel_prompt=' /root/.hermes/hermes-agent/gateway | head -100 || true
          REMOTE
      - name: Cleanup
        if: always()
        run: rm -f ~/.ssh/key
