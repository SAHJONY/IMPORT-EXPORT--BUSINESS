name: Hostinger OpenClaw 24x7 [RETIRED]

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo HOSTINGER_OPENCLAW_LEGACY_RETIRED=1
          echo USE_HOSTINGER_RECOVERY_CONTROLLER=1
          echo USE_HOSTINGER_RUNTIME_RECOVERY_V11=1
