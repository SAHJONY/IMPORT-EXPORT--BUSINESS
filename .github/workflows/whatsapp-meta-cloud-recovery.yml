name: WhatsApp Meta Cloud Recovery [RETIRED]

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo WHATSAPP_META_CLOUD_RECOVERY_RETIRED=1
          echo META_NOT_CONTROLLED=1
          echo AUTHORITATIVE_TRANSPORT=HOSTINGER_OPENCLAW
