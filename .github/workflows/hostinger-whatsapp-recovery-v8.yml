name: Hostinger WhatsApp Recovery V8 [RETIRED]

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Retired safety notice
        run: |
          echo HOSTINGER_WHATSAPP_RECOVERY_V8_RETIRED=1
          echo REASON=duplicate_sshd_shared_runtime_directory_was_unsafe
          echo USE_HOSTINGER_NATIVE_SSH_SELF_HEAL_V10=1
          echo CANONICAL_ENGINE=openclaw/hostinger-24x7/ssh-self-heal.sh
